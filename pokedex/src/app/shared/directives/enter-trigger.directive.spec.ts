import { EnterTriggerDirective } from './enter-trigger.directive';

describe('FilterEnterTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
